# Distributions

## Exponential Distribution

The exponential distribution can be used to model lifetimes, like the Geometric distribution except in continuous time as opposed to trials. It's density is given as:

$$
f(x \mid \beta) = \frac{1}{\beta} \exp(-x / \beta), x \in [0, \infty)
$$


